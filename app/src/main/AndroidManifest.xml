<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sample.neuroid.us">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:name=".MyApplicationDemo"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/nid_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeuroIdAndroidExample"
        tools:ignore="DataExtractionRules,UnusedAttribute">
        <activity
            android:name=".activities.MainActivity"
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.NIDOnlyOneFragActivity"
            android:exported="false"
            android:theme="@style/Theme.NeuroIdAndroidExample"
            android:windowSoftInputMode="stateHidden|adjustResize"/>
        <activity android:name=".activities.NIDSomeFragmentsActivity"
            android:exported="false"
            android:theme="@style/Theme.NeuroIdAndroidExample"
            android:windowSoftInputMode="stateHidden|adjustResize"/>
        <activity android:name=".activities.NIDCustomEventsActivity"
            android:exported="false"
            android:theme="@style/Theme.NeuroIdAndroidExample"/>
    </application>

</manifest>